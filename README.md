# 📱 RuStore MVP - Витрина Приложений

## Описание Проекта
Данный проект представляет собой **MVP (Minimum Viable Product)** витрины приложений для Android. Цель — создать современный и увлекательный пользовательский опыт для просмотра, поиска, фильтрации и установки приложений.

Проект разработан на **Kotlin** с использованием **Jetpack Compose** для создания пользовательского интерфейса, следуя архитектуре **MVVM** и принципам **Clean Architecture**.

## 🛠️ Стек Технологий
* **Язык:** Kotlin
* **UI Toolkit:** Jetpack Compose
* **Архитектура:** MVVM, Clean Architecture
* **Dependency Injection:** Koin
* **Networking:** Retrofit, OkHttp, Gson/Kotlinx.Serialization
* **Data Persistence:** Jetpack DataStore (Preferences)
* **Asynchronous:** Kotlin Coroutines & Flow
* **Navigation:** Jetpack Compose Navigation (с поддержкой Safe Args/Type-safe navigation)
* **Image Loading:** Coil
* **Downloading:** Android DownloadManager
* **Other Libraries:** AndroidX Core, Lifecycle, Compose Material 3

## 🏗️ Архитектура Проекта
Проект структурирован по слоям (Clean Architecture) и использует MVVM в слое представления.

1.  **`data` Layer (Слой данных):**
    * **API:** Интерфейс `AppsApi` для работы с сетью (Retrofit).
    * **DTO (Data Transfer Objects):** Классы для парсинга ответов API (`AppDto`, `AppsListDto`, `TagDto`).
    * **RemoteDataSource:** Реализует логику получения данных из сети и инициирует загрузки (`downloadApp` через `DownloadManager`).
    * **Repository:** `AppsRepository` выступает единой точкой доступа к данным, преобразует DTO в Model и обрабатывает ответы с помощью `Resource` (для обработки состояний Success/Loading/Error).
    * **Utils:** Вспомогательные классы для обработки ответов API (`safeApiResponse`) и работы с DataStore (`DataStoreManager`).
    * **Receivers:** `DownloadCompletedReceiver` обрабатывает завершение загрузки и инициирует установку APK.

2.  **`domain` Layer (Доменный слой):**
    * **Models:** Чистые Kotlin классы, представляющие бизнес-сущности (`AppModel`, `AppsListModel`, `TagModel`).
    * **State:** Enum `DownloadingState` для отслеживания состояния загрузки.

3.  **`presentation` Layer (Слой представления):**
    * **Screens:** Composable-функции для построения UI (`MainScreen`, `HomeScreen`, `CategoriesScreen`, `AppDetailsScreen`, `OnboardingScreen`).
    * **ViewModels:** Обрабатывают бизнес-логику и управляют состоянием UI (`MainViewModel`, `HomeViewModel`, `CategoriesViewModel`, `AppDetailsViewModel`). Используют Koin для инъекции зависимостей.
    * **Navigation:** Навигация реализована с использованием **Compose Navigation** и **Type-safe arguments** (`kotlinx.serialization` и `serializableNavType`).

4.  **`di` (Dependency Injection):**
    * Модули Koin (`DataModule.kt`, `PresentationModule.kt`) для настройки зависимостей (Retrofit, OkHttpClient, DataStore, Repositories, ViewModels).

## 🚀 Реализованные Функциональности
1.  **Онбординг (Onboarding):**
    * Экран показывается только при **первом запуске** приложения, используя `DataStore` для хранения флага.
    * Содержит приветственный текст и кнопку "Начать".

2.  **Главный Экран (Home Screen):**
    * Отображение **списка приложений** с сервера.
    * **Поиск** приложений (по названию) с моментальным обновлением.
    * **Фильтрация** по категориям/тегам.
    * **Индикация загрузки** (Shimmer Effect) и обработка ошибок.
    * Использование **`Flow`** для реактивного обновления данных.

3.  **Экран Категорий (Categories Screen):**
    * Отображение **списка доступных категорий/тегов** с сервера.
    * Переход на Главный экран с **пре-выбранным тегом** для фильтрации.

4.  **Экран Деталей Приложения (App Details Screen):**
    * Отображение подробной информации о приложении.
    * **Кнопка "Установить"** для запуска загрузки.
    * **Просмотр скриншотов** в полноэкранном режиме (Horizontal Pager).
    * Индикация **состояния загрузки** (`DownloadingState`).

5.  **Загрузка и Установка Приложения:**
    * Используется **Android DownloadManager** для асинхронной загрузки APK.
    * После завершения загрузки **BroadcastReceiver** (`DownloadCompletedReceiver`) перехватывает событие и инициирует **установку APK** с использованием `FileProvider` (для безопасного обмена файлами).
    * Требуются разрешения: `INTERNET`, `ACCESS_NETWORK_STATE`, `REQUEST_INSTALL_PACKAGES`, `POST_NOTIFICATIONS`.

6.  **Навигация:**
    * Используется **Type-safe Compose Navigation** для безопасной передачи аргументов (например, объекта `AppModel` на экран деталей).
    * **Bottom Navigation Bar** скрывается на экранах Онбординга и Деталей Приложения.
    * Реализована адаптивная навигация с использованием `NavigationSuiteScaffold` для поддержки различных размеров экрана (планшеты/desktop).

## 💡 Предложенный Подход и Улучшения
* **Современный UI/UX:** Использование **Jetpack Compose** и **Material 3** для создания современного, адаптивного и отзывчивого интерфейса с поддержкой темной/светлой темы.
* **Производительность:** Использование **Coroutines/Flow** для эффективной асинхронной работы и **Coil** с кешированием для загрузки изображений.
* **Надежность:** Использование паттерна **Resource** для унифицированной обработки состояний данных (загрузка, успех, ошибка) и `safeApiResponse` для обработки сетевых ошибок.
* **Безопасная Загрузка:** Использование стандартных механизмов Android (`DownloadManager`, `FileProvider`) для безопасной загрузки и установки.

📱 Store App<br>
├── data/<br>
│   ├── api/             # Интерфейсы Retrofit (AppsApi.kt)<br>
│   ├── dto/             # Объекты передачи данных (AppDto.kt, AppsListDto.kt)<br>
│   ├── receivers/       # BroadcastReceiver для завершения загрузки (DownloadCompletedReceiver.kt)<br>
│   ├── repository/      # Реализация репозиториев (AppsRepository.kt)<br>
│   ├── source/          # Источники данных (RemoteDataSource.kt)<br>
│   └── utils/           # Утилиты для работы с данными (Resource.kt, DataStoreManager.kt, Utils.kt)<br>
├── di/                  # Koin модули (DataModule.kt, PresentationModule.kt)<br>
├── domain/              # Бизнес-логика, модели (AppModel.kt, TagModel.kt)<br>
├── presentation/<br>
│   ├── categories/      # Фича: Категории (CategoriesScreen.kt, CategoriesViewModel.kt)<br>
│   ├── home/            # Фича: Главный экран (HomeScreen.kt, HomeViewModel.kt)<br>
│   │   └── details/     # Фича: Детали приложения (AppDetailsScreen.kt, AppDetailsViewModel.kt)<br>
│   ├── navigation/      # Навигационные графы и дестинации (AppNavigation.kt, HomeDestinations.kt)<br>
│   ├── onboarding/      # Фича: Онбординг (OnboardingScreen.kt)<br>
│   ├── theme/           # Цвета, темы, типографика (Theme.kt, Color.kt)<br>
│   └── utils/           # Утилиты для UI (shimmerEffect, serializableNavType)<br>
└── utils/               # Общие константы (Constants.kt)
